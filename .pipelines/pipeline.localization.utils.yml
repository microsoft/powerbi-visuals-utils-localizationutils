trigger: 
- main

variables:
  - name: branchRef
    value: 'main'
  - name: locBranchNamePrefix
    value: 'parsed_localizations_'

resources:
  repositories:
    - repository: powerbi-visuals-utils-localizationutils
      endpoint: 'pbicvloc-github'
      type: github
      name: microsoft/powerbi-visuals-utils-localizationutils
      ref: refs/heads/$(branchRef)

stages:
- stage: build
  jobs:
  - job: main
    pool:
      vmImage: 'windows-latest'

    steps:
      - checkout: powerbi-visuals-utils-localizationutils
        persistCredentials: true
      - powershell: |
          $branchName = "$(locBranchNamePrefix)" + (Get-Date -Format "yyyy-MM-dd_HH-mm-ss")
          Write-Host "##vso[task.setvariable variable=branchName]$branchName"
        name: SetVariables
        displayName: 'Set Variables'
      - task: NodeTool@0
        inputs:
          versionSpec: '18.x'
        displayName: 'Install Node.js'
      - script: |
          git config --global user.name "$(LOC_BOT_NAME)"
          git config --global user.email "$(LOC_BOT_EMAIL)"
          git config --global core.autocrlf false
          git checkout -b $(branchName)
        displayName: 'Configure Git'
      - task: AzureKeyVault@2
        inputs:
          azureSubscription: 'CustomVisualsASPManagedIdentity'
          KeyVaultName: 'CustomVisualsKeyVault'
          SecretsFilter: 'pbicvloc-gpg-azure-devops-private-key,LOC-APP-ID,LOC-APP-PRIVATE-KEY'
          RunAsPreJob: false
        displayName: 'Fetch GPG signing key and GitHub App credentials from Key Vault'
      - task: PowerShell@2
        displayName: 'Generate GitHub App Access Token'
        name: getGitHubToken
        inputs:
          targetType: 'filePath'
          filePath: '.pipelines/get-github-app-token.ps1'
          arguments: >-
            -AppId "$(LOC-APP-ID)"
            -RepoOwner "microsoft"
            -RepoName "powerbi-visuals-utils-localizationutils"
            -OutputVariableName "LOC_APP_TOKEN"
          pwsh: true
        env:
          GITHUB_APP_PRIVATE_KEY: $(LOC-APP-PRIVATE-KEY)
      - powershell: |
          # Import the bot's private GPG signing key (passphrase-less) and enable signing.
          # The secret is stored base64-encoded to survive newline handling through Key Vault
          # and the pipeline variable system; decode it to a binary-safe file before importing.
          $keyPath = Join-Path $env:AGENT_TEMPDIRECTORY "pbicvloc-signing-key.asc"
          try {
            [IO.File]::WriteAllBytes($keyPath, [Convert]::FromBase64String($env:GPG_KEY))
          } catch {
            throw "Secret is not valid base64. Re-store the GPG private key base64-encoded in Key Vault."
          }

          gpg --batch --import $keyPath
          $importExit = $LASTEXITCODE
          Remove-Item $keyPath -Force -ErrorAction SilentlyContinue
          if ($importExit -ne 0) { throw "Failed to import GPG key" }

          $keyId = gpg --list-secret-keys --with-colons | Select-String '^sec' | ForEach-Object { ($_ -split ':')[4] } | Select-Object -First 1
          if (-not $keyId) { throw "No GPG secret key found after import" }
          Write-Host "Imported signing key: $keyId"

          git config --global user.signingkey $keyId
          git config --global commit.gpgsign true
          git config --global gpg.program gpg
        displayName: 'Import GPG signing key'
        env:
          GPG_KEY: $(pbicvloc-gpg-azure-devops-private-key)
      - task: OneLocBuild@2
        env:
          SYSTEM_ACCESSTOKEN: $(System.AccessToken)
        inputs:
          locProj: 'LocProject.json'
          outDir: '$(Build.SourcesDirectory)'
          isCreatePrSelected: true
          repoType: 'gitHub'
          prSourceBranchPrefix: 'locfiles'
          dependencyPackageSource: 'https://powerbi.pkgs.visualstudio.com/PowerBIClients/_packaging/VisualsMonitorFeed/nuget/v3/index.json'
          packageSourceAuth: 'patAuth'
        displayName: 'Run OneLocBuild'
      - script: npm install
        displayName: 'Install dependencies'
      - script: npm run parseNewLocalizations
        displayName: 'Parse new localizations'
      - script: git status --short
        displayName: 'Git status'
      - powershell: |
          $matchingPRs = gh pr list --repo "microsoft/powerbi-visuals-utils-localizationutils" `
            --author "@me" `
            --state open `
            --json number,headRefName | ConvertFrom-Json | `
            Where-Object { $_.headRefName -match "$(locBranchNamePrefix)" }

          if (-not $matchingPRs) {
            Write-Host "No open pull requests matching the branch pattern '$(locBranchNamePrefix)'."
          } else {
            foreach ($pr in $matchingPRs) {
              Write-Host "Attempting to close PR #$($pr.number)"
              gh pr close $($pr.number) --repo "microsoft/powerbi-visuals-utils-localizationutils" --delete-branch
              if ($LASTEXITCODE -eq 0) {
                Write-Host "Successfully closed PR #$($pr.number) and deleted its branch"
              } else {
                Write-Host "Failed to close PR #$($pr.number) - Please check repository permissions"
              }
            }
          }
        displayName: 'Delete old pull requests'
        env:
          GH_TOKEN: $(LOC_APP_TOKEN)
      - powershell: |
          git add localizations

          git diff --cached --quiet
          if ($LASTEXITCODE -eq 0) {
            Write-Host "No localization changes to commit"
            exit 0
          }

          Write-Host "Changes detected in localizations/, proceeding with commit"
          git commit -S -m "Update localizations"
          if ($LASTEXITCODE -ne 0) { throw "git commit failed" }

          # Authenticate the push and subsequent gh CLI calls with the GitHub App installation token.
          # x-access-token is the required username for App installation tokens.
          git remote set-url origin "https://x-access-token:$(LOC_APP_TOKEN)@github.com/microsoft/powerbi-visuals-utils-localizationutils.git"

          git push origin $(branchName)
          if ($LASTEXITCODE -ne 0) { throw "git push failed" }

          gh pr create `
            --base $(branchRef) `
            --title "Update localizations $(Get-Date -Format 'yyyy-MM-dd')" `
            --body "Automated PR to update localizations"

          if ($LASTEXITCODE -ne 0) {
            Write-Host "Failed to create PR, it might already exist"
          }
        displayName: 'Process and Push Localizations'
        continueOnError: true
        env:
          GH_TOKEN: $(LOC_APP_TOKEN)
          # Suppress any credential prompts during automated git operations.
          GIT_ASKPASS: echo
          GIT_TERMINAL_PROMPT: '0'
      - task: PublishPipelineArtifact@1
        inputs:
          targetPath: '$(Build.SourcesDirectory)'
          artifact: 'drop'
          publishLocation: 'pipeline'
        displayName: 'Publish Pipeline Artifact'
